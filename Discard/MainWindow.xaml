<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Height="720" Width="1280" MinWidth="650" MinHeight="700" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen" WindowStyle="None" Background="Transparent">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" />
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid>
            <Border CornerRadius="5">
                <Border.Background>
                    <LinearGradientBrush EndPoint="0.5,1">
                        <GradientStop Color="#1e2124" />
                        <GradientStop Color="#282b30" Offset="1.0" />
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <!-- Status Bar -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 15 15 0">
                        <TextBlock Text="17:19" Foreground="#fdf9f6" FontSize="26" VerticalAlignment="Center" />

                        <StackPanel Margin="13 0" VerticalAlignment="Center">
                            <TextBlock Text="17, April" Foreground="#f8fefa" FontSize="10" />
                            <TextBlock Text="Søndag" Foreground="#f8fefa" FontSize="10" />
                        </StackPanel>

                        <icon:PackIconMaterial Kind="WeatherSunset" Foreground="#fdf9f6" Width="25"
                                               VerticalAlignment="Center" />
                    </StackPanel>

                    <!-- Page Title -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Hello" Style="{StaticResource PageTitle}" />

                    <Grid Grid.Row="1" Margin="0 30 0 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Friend List -->
                        <Border Grid.Row="1" Grid.Column="0" Height="600" Width="100" CornerRadius="25"
                                HorizontalAlignment="Left">
                            <Border.Background>
                                <LinearGradientBrush EndPoint="0.5,1">
                                    <GradientStop Color="#7289da" />
                                    <GradientStop Color="#7289da" Offset="1.0" />
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>

                        <!-- Page Content -->
                        <Border CornerRadius="55" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="1"
                                Grid.Column="1"
                                Background="red" Height="600" 
                                Width="1200" Margin="10 0 20 0">
                        </Border>

                    </Grid>

                </Grid>
            </Border>

        </Grid>
    </Grid>
</Window>